# Date to used as validation
reference_start_date: '2018-06-01'

# Metric used for evaluating model performance (ROC AUC score)
metric: 'roc_auc_score'

# Flag indicating whether higher values of the metric are better (True in this case)
higher_is_better: True