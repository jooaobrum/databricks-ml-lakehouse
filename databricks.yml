bundle:
  name: databricks-ml-lakehouse

include:
- resources/data-lake-jobs/*.yml

artifacts:
  default:
    type: whl
    build: uv build
    path: .


variables:
  git-sha:
    description: git-sha
    default: abcd
  branch:
    description: branch
    default: main
  schedule_pause_status:
    description: schedule pause status
    default: UNPAUSED

targets:
  dev:
    cluster_id: 0610-174950-70xr60bt
    default: true
    mode: development
    workspace:
      host: https://adb-2625093468221227.7.azuredatabricks.net
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.target}/${bundle.name}
  qa:
    cluster_id: 0610-174950-70xr60bt
    default: false
    mode: development
    workspace:
      host: https://adb-2625093468221227.7.azuredatabricks.net
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.target}/${bundle.name}
  prod:
    default: false
    workspace:
      host: https://adb-2625093468221227.7.azuredatabricks.net
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.target}/${bundle.name}
